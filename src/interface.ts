export interface ICameraProps {
  width?: string;
  circle?: boolean;
  borderWidth?: string;
  borderColor?: string;
}

export interface IConfig {
  size: number;
  circle: boolean;
  hasBorder: boolean;
  borderWidth: number;
  borderColor: string;
}
